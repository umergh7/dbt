version: 2

models:
  - name: agg_cat_facts
    description: >
      agg cat facts
    columns:
      - name: fact
        description: fact for cat
        tests:
          - not_null
      - name: created_at
        description: ingestion date

